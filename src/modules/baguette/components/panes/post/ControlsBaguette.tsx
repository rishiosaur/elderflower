import { motion } from "framer-motion";
import { BaguetteComponent, BaguetteTypes } from "../../base";

export const ControlsBaguette: BaguetteComponent<BaguetteTypes.Controls> = (
  props
) => {
  return <motion.div>hi</motion.div>;
};
