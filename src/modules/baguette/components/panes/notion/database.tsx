import { BaguetteComponent, BaguetteTypes } from "../../base";

export const NotionDatabaseBaguette: BaguetteComponent<
  BaguetteTypes.NotionDatabase
> = (props) => {
    
    return (
        return <motion.div>
            {
                props
            }

        </motion.div>
    )
};
